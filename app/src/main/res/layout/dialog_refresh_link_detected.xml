<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="4dp"
    android:paddingBottom="16dp">

    <TextView
        android:id="@+id/tv_refresh_link_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="?attr/colorOnSurface"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.3"
        android:alpha="0.75" />

    <RadioGroup
        android:id="@+id/radio_group_refresh"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp">

        <RadioButton
            android:id="@+id/radio_update_existing"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:text="@string/refresh_link_option_update_existing"
            android:textColor="?attr/colorOnSurface"
            android:textSize="15sp"
            android:buttonTint="?attr/colorPrimary"
            android:checked="true" />

        <RadioButton
            android:id="@+id/radio_add_new"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:text="@string/refresh_link_option_add_new"
            android:textColor="?attr/colorOnSurface"
            android:textSize="15sp"
            android:buttonTint="?attr/colorPrimary" />

    </RadioGroup>

</LinearLayout>
