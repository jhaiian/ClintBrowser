<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="4dp"
    android:minHeight="52dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/suggestionIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_search_24"
        android:tint="?attr/clintIconTint"
        android:alpha="0.7"
        android:scaleType="fitCenter"
        android:contentDescription="@string/suggestion_icon_desc" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/suggestionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/suggestionUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="?attr/clintSecondaryTextColor"
            android:maxLines="1"
            android:ellipsize="end"
            android:visibility="gone"
            android:contentDescription="@string/suggestion_url_desc" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/btnFill"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_arrow_back_24"
        android:tint="?attr/clintIconTint"
        android:alpha="0.6"
        android:rotation="135"
        android:contentDescription="@string/suggestion_fill_desc" />

    <ImageButton
        android:id="@+id/btnDelete"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close_24"
        android:tint="?attr/clintIconTint"
        android:alpha="0.6"
        android:visibility="gone"
        android:contentDescription="@string/history_delete_desc" />

</LinearLayout>
